<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:paddingStart="16dp"
    android:paddingEnd="8dp">

    <TextView
        android:id="@+id/tvDotsTop"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:gravity="center"
        android:text="⋮"
        android:textColor="?attr/colorPrimary"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/markerContainer"
        app:layout_constraintEnd_toEndOf="@+id/markerContainer"
        app:layout_constraintStart_toStartOf="@+id/markerContainer"
        app:layout_constraintTop_toTopOf="parent" />

    <FrameLayout
        android:id="@+id/markerContainer"
        android:layout_width="32dp"
        android:layout_height="32dp"
        app:layout_constraintBottom_toBottomOf="@+id/cardDetails"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/cardDetails">

        <TextView
            android:id="@+id/tvIndexLetter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_stop_marker"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="A"
            tools:visibility="visible" />

        <View
            android:id="@+id/viewOriginMarker"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_stop_origin"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ivDestinationMarker"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="Ziel"
            android:src="@android:drawable/ic_menu_mylocation"
            android:visibility="gone"
            app:tint="@android:color/holo_red_dark" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvDotsBottom"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:gravity="center"
        android:lineSpacingExtra="-4dp"
        android:text="⋮\n⋮\n⋮"
        android:textColor="?attr/colorPrimary"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/markerContainer"
        app:layout_constraintStart_toStartOf="@+id/markerContainer"
        app:layout_constraintTop_toBottomOf="@+id/markerContainer" />

    <!-- Die Karte mit den Stopp-Details -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardDetails"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/markerContainer"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp">

            <TextView
                android:id="@+id/tvAddress"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textStyle="bold"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Hauptstraße 1, 72108 Rottenburg" />

            <!-- FK-TODO: llEditableFields hat nur ein einziges Element, den spinnerDeliveryGroup, der aus llEditableFields herausgeholt werden kann, so dass llEditableFields entfernt werden kann -->
            <LinearLayout
                android:id="@+id/llEditableFields"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                app:layout_constraintTop_toBottomOf="@+id/tvAddress">

                <Spinner
                    android:id="@+id/spinnerDeliveryGroup"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp" />
            </LinearLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</androidx.constraintlayout.widget.ConstraintLayout>