<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ExtensionsForMaps" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ExtensionsForMaps" parent="Base.Theme.ExtensionsForMaps" />

    <style name="Theme.ExtensionsForMaps.Dialog" parent="Theme.Material3.DayNight.Dialog" />

    <style name="Theme.ExtensionsForMaps.Transparent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>